// --- Directions
// Given an integer, return an integer that is the reverse
// ordering of numbers.
// --- Examples
//   reverseInt(15) === 51
//   reverseInt(981) === 189
//   reverseInt(500) === 5
//   reverseInt(-15) === -51
//   reverseInt(-90) === -9

function reverseInt(n) {
}

module.exports = reverseInt;

// const result = n.toString().split('').reverse().join('');
// return parseInt(result) * Math.sign(n);


// let number = n < 0 ? '-' : '';
// const result = n.toString().split('').reduce((first, next) => next + first);
// number += result;
// return parseInt(number);